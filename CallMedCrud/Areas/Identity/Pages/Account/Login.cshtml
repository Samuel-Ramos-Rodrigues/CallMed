@page
@model MKSANCrud.Areas.Identity.Pages.Account.LoginModel
@{ Layout = "_AuthLayout"; ViewData["Title"] = "Entrar"; }
<main class="identity-shell login-shell">
    <section class="identity-showcase">
        <a asp-area="" asp-controller="Home" asp-action="Index" class="app-brand"><img src="~/images/logo-callmed-mark.png" alt="" /><span>CallMed<small>CUIDADO EM CONEXÃO</small></span></a>
        <div class="identity-copy"><span class="eyebrow">SEU TEMPO IMPORTA</span><h1>Mais perto<br />do seu<br /><em>próximo cuidado.</em></h1><p>Consultas, orientações e atendimento em um lugar que acompanha você.</p></div>
        <div class="identity-note-line"><svg aria-hidden="true"><use href="/icons/callmed-ui.svg#calendar"></use></svg><span>Uma agenda que conecta<br /><strong>pacientes e profissionais.</strong></span></div>
        <small>CallMed · simples desde o primeiro acesso</small>
    </section>
    <section class="identity-panel">
        <div class="identity-card">
            <a class="identity-back" asp-area="" asp-controller="Home" asp-action="Index">← Voltar ao início</a>
            <div class="identity-card-header"><span class="eyebrow">BEM-VINDO À CALLMED</span><h2>Bom ter você aqui.</h2><p>Entre para acompanhar suas consultas ou acessar seu espaço de trabalho.</p></div>
            <form method="post" class="identity-form">
                        <input type="hidden" name="returnUrl" value="@Model.ReturnUrl" />
                        <div asp-validation-summary="ModelOnly" class="mensagem erro"></div>
                        <div class="input-group">
                            <label asp-for="Input.Usuario"></label>
                            <input asp-for="Input.Usuario" autocomplete="username" autofocus placeholder="CPF ou e-mail" />
                            <span asp-validation-for="Input.Usuario" class="field-error"></span>
                        </div>
                        <div class="input-group">
                            <label asp-for="Input.Password"></label>
                            <div class="password-wrap">
                                <input asp-for="Input.Password" id="login-password" autocomplete="current-password" placeholder="Digite sua senha" />
                                <button class="password-toggle" id="login-password-toggle" type="button">Mostrar</button>
                            </div>
                            <span asp-validation-for="Input.Password" class="field-error"></span>
                        </div>
                        <div class="identity-options">
                            <label><input asp-for="Input.RememberMe" /> Lembrar acesso</label>
                            <a asp-page="./ForgotPassword">Esqueci minha senha</a>
                        </div>
                        <button type="submit" class="identity-submit">Entrar na CallMed</button>
                    </form>
            <div class="identity-divider"><span>Ainda não tem acesso?</span></div>
            <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl" class="identity-secondary">Criar conta de paciente <span aria-hidden="true">↗</span></a>
            <p class="identity-footnote">Faz parte da equipe? Use o acesso fornecido pela clínica.</p>
        </div>
    </section>
</main>
@section Scripts {
<script src="~/js/login.js" asp-append-version="true"></script>
}
